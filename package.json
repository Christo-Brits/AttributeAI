{
  "name": "attributeai",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter web dev",
    "dev:ext": "pnpm --filter extension dev",
    "build": "pnpm -r --parallel build",
    "lint": "pnpm -r lint",
    "test": "vitest run --no-colors",
    "test:watch": "vitest"
  }
}
